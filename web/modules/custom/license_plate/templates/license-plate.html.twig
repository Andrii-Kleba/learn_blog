{% if concatenated %}
  <span class="license_plate">{{ code }} {{ number }}</span>
{% else %}
  <span class="license_plate-code">{{ code }}</span>
  <span class="license_plate-number">{{ number }}</span>
{% endif %}
